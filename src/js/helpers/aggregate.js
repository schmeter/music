import api from '../../config/api.json';
import app from '../../config/app.json';
import i18n from '../../config/i18n.json';
import routes from '../../config/routes';
import audio from '../../shared/audio.json';
import event from '../../shared/event.json';
import video from '../../shared/video.json';

export const config = {
  api,
  app,
  i18n,
  routes,
};

export const data = {
  audio,
  event,
  video,
};
